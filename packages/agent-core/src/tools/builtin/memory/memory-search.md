# MemorySearch

Search cross-session memory using BM25 over content and weighted tags. Returns matching entries with ids so you can call `MemoryRead` for full content.

Use to find prior decisions, preferences, or notes before asking the user to repeat themselves.
